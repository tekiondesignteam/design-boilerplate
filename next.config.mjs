/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: '/design-boilerplate',
    images: {
        unoptimized: true,
    },
};

export default nextConfig;
